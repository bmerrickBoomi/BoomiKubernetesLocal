apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: apim-{{name}}-claim
  namespace: apim-{{name}}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: apim-{{name}}-storage
  resources:
    requests:
      storage: 5Gi
