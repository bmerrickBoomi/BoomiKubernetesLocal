apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openldap-{{port}}-claim
  namespace: addons-openldap-{{port}}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: openldap-{{port}}-storage
  resources:
    requests:
      storage: 5Gi
