apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: molecule-{{name}}-claim
  namespace: molecule-{{name}}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: molecule-{{name}}-storage
  resources:
    requests:
      storage: 5Gi
